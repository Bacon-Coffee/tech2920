# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目性质

这是 **TECH 2390 课程的研究项目**，不是一个软件产品。最终交付物是两份文档，不是可运行的系统：

1. **IEEE 格式研究论文**（占 7 分）：6–8 整页、10pt、双倍行距、≥10 篇近 4 年的权威文献、Word 管理引用与参考文献
2. **PowerPoint 演示**（占 3 分）：10–15 分钟课堂演讲

仿真代码、实验结果、图表都是**论文的素材**，不是交付物本身。任何写代码的工作必须服务于"能进论文 / 能进 PPT"这个最终目的。

> ⚠️ 选题确认：用户选的"城市交通信号控制算法对比"不在课程预设题目列表里，最接近的是 *Any topics on Robotics and driverless automobile*。如果用户尚未向老师确认选题获批，应在动手前提醒一次。

## 研究问题（论文的核心命题）

> 在真实城市路网上，5 种代表性信号控制算法在**正常流量**、**不均衡流量**、**事故场景**三种条件下，哪种在「性能 — 鲁棒性 — 实现复杂度」三维度上综合最优？

论文标题、摘要、结论必须围绕这个问题展开，不要扩散到"AI 在交通中的应用综述"等空泛主题。

## 实验设计（论文 Section III 的骨架）

### 自变量
| 维度 | 取值 | 备注 |
|---|---|---|
| 算法 | Fixed-Time / Actuated / SOTL / Max-Pressure / DQN | 前两个是 baseline，Max-Pressure 是 throughput-optimal 的白盒标杆，DQN 是黑盒标杆 |
| 路网 | 4×4 合成网格 + 1 个真实路口 | 真实路口选用户所在城市的数据 |
| 场景 | 正常 / 不均衡（主:支 = 5:1）/ 事故（50% 时刻一车道封闭 10min） | 后两者用于鲁棒性测试 |
| 流量强度 | 200, 400, 600, 800, 1000 veh/h/lane | 5 档 |
| 随机种子 | 1–10 | 每组配置跑 10 次 |

**总仿真次数：5 × 2 × 3 × 5 × 10 = 1500**。SUMO 单次 1–3 分钟，可并行通宵跑完。

### 因变量（7 个指标）
平均行驶时间、平均等待时间、平均停车次数、吞吐量、CO₂ 排放（SUMO HBEFA 模型）、最大队列长度、Jain's fairness index。

### 控制变量（必须固定）
- 跟车模型 Krauss、换道模型 LC2013、`sigma=0.5`
- 仿真时长 3600s，预热 600s 后才开始统计
- **Common Random Numbers (CRN)**：场景 A 用种子 [1–10]，场景 B 也用 [1–10]，保证不同算法面对完全相同的随机性 —— 这是方差缩减的标准做法，写代码时必须强制这一点

### 统计分析（区分本科水平 vs 论文水平的关键）
不能只报均值。每一个对比结论必须包含：
- **配对 t 检验**（同种子配对，不是独立样本 t）
- **Cohen's d** 效应量
- **95% 置信区间** —— 所有图表的均值必须带误差棒
- **双因素 ANOVA**（场景 × 算法的交互效应）

`scipy.stats` 几行就够，但写出来论文立刻"严肃"。

## 执行顺序（用户已定，请遵守）

1. **跑最小可行实验**：4×4 网格 + Fixed-Time + 单一流量强度，验证流水线打通
2. **逐步加算法、加场景**，按算法→场景→种子的顺序填满 1500 次矩阵
3. **统计分析与画图**：scipy.stats 配对 t / Cohen's d / 双因素 ANOVA，6 张核心图全部产出
4. **最后再写论文**：Section II/III/IV/V 等所有写作环节放在实验数据与图全部就绪之后。仿真代码、实验结果、统计图先跑完再开始动笔，**不要边跑边写**——写作时数据全在桌上才能保证数字、图、结论三者一致；中途写作很容易因为后续实验数据更新导致重写。PPT 同理：脚本、图、视频都齐了再做。

不要一开始就并行写五个算法。先打通一条端到端的窄通道。

## 技术栈（尚未搭建）

代码目录还不存在。当用户开始动手时，建议结构：

```
sim/         SUMO 网络文件 (.net.xml, .rou.xml, .sumocfg) 与流量生成脚本
controllers/ 5 种算法实现（每种一个文件，共用同一个 abstract base class）
runner/      实验编排：参数矩阵、并行调度、结果落盘 (Parquet/CSV)
analysis/    scipy.stats 统计检验 + matplotlib 出图（论文用 PDF，PPT 用 PNG）
paper/       论文 .docx、参考文献 .bib/Word source manager 导出
results/     原始仿真输出与汇总表（不进 git 或用 LFS）
```

预期依赖：`sumolib` + `traci`（SUMO Python API）、`numpy`、`pandas`、`scipy`、`matplotlib`。RL 部分用 `stable-baselines3` 的 DQN 即可，不要从零实现。

**代码骨架落地后，请把构建/运行/单测命令补到本节。** 目前没有可运行命令，不要编造。

## 语言与格式约定

- 论文正文与图表标签：**英文**（IEEE 格式要求）
- 项目内 Markdown 文档（本文件、设计笔记等）：**中文**（沿用全局 CLAUDE.md 偏好）
- 代码注释：英文
- Commit message：中英文均可，但保持单条一致

## 论文写作硬约束（来自课程要求，违反直接挂科）

- ≥10 篇文献，全部发表于**近 4 年**（即 2022 年后），来源限期刊或权威网站
- 每一条参考文献都必须在正文有对应引用，否则按抄袭处理
- 引用与参考文献用 **Word 的 Source Manager** 管理，不要手敲

写文献综述时，Max-Pressure 必引 Varaiya 系列、DQN 必引 PressLight / CoLight / MPLight 这条线索（这些都有 2022 年后的扩展工作）。
